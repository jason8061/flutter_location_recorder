import 'package:flutter/material.dart';

class ViewCsv extends StatelessWidget {
  final String path;

  const ViewCsv({Key key, this.path}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
